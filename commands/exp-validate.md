---
description: Statistical interpretation + reproducibility verification
---

Trigger the `experiment-agent` skill in `validate` mode. Scan for statistical content (p-values, CIs, effect sizes), interpret results, check 11 fallacy types, and optionally re-run code for reproducibility verification.

Mode reference: `SKILL.md` § validate.
Skill entry: `SKILL.md`.
